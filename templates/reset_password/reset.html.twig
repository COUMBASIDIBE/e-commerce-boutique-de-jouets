{% extends 'base.html.twig' %}

{% block title %}Réinitialiser son mot de passe
{% endblock %}

{% block content %}
	<h1>Réinitialiser son mot de passe</h1>

	{{ form_start(resetForm) }}
	{{ form_row(resetForm.plainPassword) }}
	<button class="btn btn-secondary">Réintialiser son mot de passe</button>
	{{ form_end(resetForm) }}
{% endblock %}
